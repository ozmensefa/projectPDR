### Bu projenin çalışması için aşağıdaki kütüphanelerin yüklenmesi gereklidir. ###
Flask==2.2.5
Werkzeug==2.2.2
opencv-python
mediapipe
deepface
openai-whisper
librosa
matplotlib
google-generativeai

# GPU Desteği için PyTorch (CUDA) - DeepFace için gerekli
torch>=1.13.0
torchaudio>=0.13.0
torchvision>=0.14.0

# GPU Desteği için TensorFlow - DeepFace'in ana backend'i
# CUDA 12.x uyumlu TensorFlow
# Not: MediaPipe ile uyumluluk için 2.17.0 kullanılıyor
tensorflow==2.17.0

# Diğer bağımlılıklar
soundfile
Flask-Login
Flask-SQLAlchemy
Flask-WTF
email-validator
markdown
numpy
resend
python-dotenv

# Asenkron Task Queue - Arka Plan İşlemleri
celery>=5.3.0
redis>=5.0.0
flask-socketio>=5.3.0
python-socketio>=5.9.0
eventlet>=0.33.0

# GPU KURULUM TALİMATLARI:
# 
# 1. NVIDIA GPU (CUDA) için - ÖNERILEN YÖNTEM:
#    a) CUDA Toolkit 12.x kurun: https://developer.nvidia.com/cuda-downloads
#    b) Sonra şu komutu çalıştırın:
#       pip install tensorflow[and-cuda]>=2.15.0
#
# 2. Eğer CUDA kurulumu sorun çıkarırsa, alternatif:
#    pip uninstall tensorflow tensorflow-gpu
#    pip install tensorflow==2.15.0
#    pip install tensorflow[and-cuda]
#
# 3. GPU desteği doğrulaması:
#    python -c "import tensorflow as tf; print('GPU:', len(tf.config.list_physical_devices('GPU')))"
#
# 4. Opsiyonel GPU Desteği Paketleri:
#
# AMD GPU (DirectML) - Windows için:
# pip install tensorflow-directml-plugin
#
# Intel GPU (DirectML) - Windows için:
# pip install torch-directml
#
# Genel GPU (OpenCL) - Tüm platformlar:
# pip install pyopencl